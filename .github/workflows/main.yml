name: "CI"

on:
  push:
    branches:
      - main
  pull_request: {}

jobs:
  build:
    container: marcusgrass/pgwm-check:latest
    steps:
        uses: actions/checkout@v3
      - name: Run check script
        run: cargo test

