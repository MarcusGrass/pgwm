name: "CI"

on:
  push:
    branches:
      - main
  pull_request: {}

jobs:
  check:
    runs-on: ubuntu-latest
    container: marcusgrass/pgwm-check:latest
    steps:
      - uses: actions/checkout@v3
      - name: Run clippy on all permutations
        run: cargo hack clippy --feature-powerset -- -D warnings
      - name: Run cargo deny
        run: cargo deny --all-features --frozen --locked check
      - name: Run test on core
        run: cargo hack test -p pgwm-core --feature-powerset
      - name: Run test on project default features
        run: cargo test
